/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Save_UsuarioService } from './save_Usuario.service';

describe('Service: Save_Usuario', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Save_UsuarioService]
    });
  });

  it('should ...', inject([Save_UsuarioService], (service: Save_UsuarioService) => {
    expect(service).toBeTruthy();
  }));
});
